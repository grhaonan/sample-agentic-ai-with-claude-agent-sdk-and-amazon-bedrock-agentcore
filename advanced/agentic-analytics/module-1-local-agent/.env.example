# Module 1 — Local text-to-SQL agent
# Copy to `.env` (gitignored).

# Use Amazon Bedrock as the model provider for the Claude Agent SDK.
CLAUDE_CODE_USE_BEDROCK=1

# Bedrock model IDs (no model= is hardcoded in the agent).
ANTHROPIC_MODEL=global.anthropic.claude-opus-4-6-v1
ANTHROPIC_SMALL_FAST_MODEL=global.anthropic.claude-haiku-4-5-20251001-v1:0

# AWS region — auto-detected from the environment by setup.sh.
# Uncomment to override:
# AWS_REGION=us-west-2

# Athena data layer created by Module 0. ATHENA_OUTPUT_LOCATION + the bucket use
# your account id; fill in after Module 0 (it prints the bucket name).
ATHENA_DATABASE=student_analytics
# ATHENA_OUTPUT_LOCATION=s3://student-analytics-agent-<account_id>/athena-results/

# AWS credentials come from your standard AWS config (aws configure / SSO / env).
